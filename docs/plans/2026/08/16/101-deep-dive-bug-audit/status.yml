plan: 101-deep-dive-bug-audit
title: Deep-dive bug audit — twelve sweeps, one plan
status: done
created_by: sebi
worked_by: "claude (hive of 4 per PR, one checkout)"
owner: sebi
percent: 100
current_focus: >
  COMPLETE. Fifteen slices, eight PRs: 05/10 = #101; 07 = #102/#103/#104; 01/09 = #105; 04 = #106;
  02/06 = #107/#110/#112; 03 = #113; 11/14 = #114; 08 = #116; 15 = #117; 12 = #118; 13 = #119.
  Error codes 387 -> 417. Twelve new gate checks built across the sweep, four of them in the last
  slice alone, because the recurring finding was never a missing rule — it was a correct rule one
  file away that did not cover the path that mattered. Five slices closed under 100% and every
  remainder is named in the deferred blocks below or filed: #120 (the MCP tool name split — CLOSED
  on branch fix/mcp-tool-name-unify: toToolName deleted from action and query, the export name
  wins on every surface, packages/mcp/src/cross-surface.test.ts pins it),
  #121 (a fresh app cannot run four generators; its first migration is wrong four ways),
  #122 (two shipped fix: lines that resolve and still do the wrong thing).
slices:
  - file: 05-gate-and-scripts.md
    tier: 0
    status: done
    percent: 100
    pr: 101
  - file: 10-tests.md
    tier: 0
    status: done
    percent: 100
    pr: 101
  - file: 07-security.md
    tier: 0
    status: done
    percent: 100
    prs: [102, 103, 104]
  - file: 01-tier01-bugs.md
    tier: 0
    status: done
    percent: 100
    pr: 105
  - file: 09-logic-edge-cases.md
    tier: 0
    status: done
    percent: 100
    pr: 105
  - file: 04-projection-contract.md
    tier: 0
    status: done
    percent: 100
    pr: 106
    note: >
      percent is #106's own scope, not the slice's entry list. Re-derived against the code
      2026-08: six entries closed or void and one mostly closed, THREE still open — `.job()` has no consumer
      (action/job-handle.ts:16), the query client encodes what coerceQuery cannot decode
      (query/client.ts:151, reproduced), coerceInput is exported with no caller
      (schema/coerce.ts:149) — plus all five enforcement gaps. The slice file carries a status
      table; one entry there is VOID, not open: `normalize()` throws and always did by the time
      the slice landed.
  - file: 02-tier23-bugs.md
    tier: 2
    status: done
    percent: 100
    prs: [107, 110, 112]
  - file: 06-concurrency-lifecycle.md
    tier: 2
    status: done
    percent: 100
    prs: [107, 110, 112]
  - file: 03-tier45-bugs.md
    tier: 4
    status: done
    percent: 99
    pr: 113
  - file: 11-deploy-ci.md
    tier: 5
    status: done
    percent: 100
    pr: 114
  - file: 14-agent-dx.md
    tier: 5
    status: done
    percent: 100
    pr: 114
  - file: 08-architecture.md
    tier: 0
    status: done
    percent: 85
    pr: 116
  - file: 12-tracked-apps.md
    tier: 5
    status: done
    percent: 90
    pr: 118
  - file: 15-dead-code-drift.md
    tier: 0
    status: done
    percent: 85
    pr: 117
  - file: 13-docs-drift.md
    tier: 5
    status: done
    percent: 100
    pr: 119
evidence:
  - pr: 101
    merged: 290dd96
    slices: [05, 10]
    gate: "14 of 17 passed, 3 skipped (drift, contract-diff, budgets)"
    falsified:
      - "10-1: `bun run test` was NOT red by shard byte-packing. Every shard already runs
         --isolate, which fully prevents the render-plugin leak; sharding by package would
         have fixed nothing and cost ~30% on the biggest bin. The divergence was one missing
         flag on root package.json's `test` script."
      - "05-5: packages/cli/src/source-files.ts:11 ALREADY contained scripts/**. The narrow
         list was scripts/boundaries.ts's own SOURCE_PATTERNS."
      - "10-L: '/e2e/' is not an expressible bun filter (bun 1.3.14: 'had no matches').
         The working form is 'e2e/'."
    deferred:
      - "282 typecheck errors in .test.ts across 110 files / 24 packages — every package
         tsconfig excludes src/**/*.test.ts. Measured, not fixed. Known-Gaps row landed."
      - "serial live/e2e steps still run without --isolate; measured 28s -> 56s. Recorded."
      - "X_PACKAGE_UNREFERENCED fail-opens on a JSONC root tsconfig (comment => parse
         returns undefined => rule reads it as 'no project references')."
    new_codes: [X_REFERENCE_APP_NO_FLOOR, X_SCAFFOLD_GATE_RED, X_PACKAGE_UNREFERENCED]
  - pr: 110
    slices: [02, 06]
    gate: "14 of 17 passed, 3 skipped (drift, contract-diff, budgets); app gate green, every pin
           holds — examples/dummy 10/17 (7 red, 7 pinned), social-media-clone 14/17 (3 red, 3 pinned)"
    falsified:
      - "02-3b: 'reverse the Redis set ordering' is HALF a fix and worse alone. SADD-then-SET with
         no re-check lets the bust SREM the membership and the later SET publish a row unreachable
         by ANY tag — permanently uninvalidatable. Ships with an SISMEMBER re-check."
      - "02-cursor: fixing `queryHash` alone was too narrow. `fingerprint` also backs
         `cacheKeyFor`, so the 32-bit hash over client-chosen input was ALSO the shared cache
         entry key — strictly worse than the cursor case the sweep named."
      - "06-attempt: repeated suspensions cannot drive `attempt` negative. `nack` is fenced on
         state === 'running', which only `claim` sets, and `claim` increments."
      - "06-deadline: `configureLifecycle({deadlineMs})` is NOT unused — http/src/server.ts:97
         declares one on every createServer. And X_SHUTDOWN_TIMEOUT already existed."
      - "02-dates: the bare-Date.now() list in the cache package is EMPTY — fixed in #105/#106/#107.
         Only `remember` bypassing assertTtl was real."
    decided:
      - "The drain deadline flipped from opt-in to bounded-by-default (25s). Built opt-in as
         briefed, but `jobs` and `realtime` declare no budget, so the proven symptom — a worker pod
         SIGKILLed mid-job — stayed unfixed. A mechanism that does not fix the symptom while
         claiming the deadline works is a claim wider than what is enforced."
      - "Colocated test fakes ship in the tarball. Four now do; a one-off `files` negation in one
         package invents a convention nothing else follows. If it matters it is one sweep over all
         29 package.json, and it belongs to the dead-code slice."
    deferred:
      - "The cache fill fence is PER PROCESS — two pods still interleave a load on one with a write
         and bust on the other. Cross-node needs a Redis-side epoch and a wire change. Known-Gaps
         row landed."
      - "`createCacheStack` has zero production callers, so its copy of the fence is dormant. The
         live fenced path is runQuery -> readRows -> readThrough -> fill."
      - "`recentTierFailures()` has no reader outside cache, and /_x's invalidations source is
         `unwired` — routed to slice 03."
      - "`invalidateWireTags` has zero callers but is the wire-form door `x cache bust` will call.
         Not dead; recorded for slice 15 to weigh."
      - "`cache.scope` is auditable by grep, not by `x queries describe` — publishing it on
         QueryDescriptor ripples into both tracked apps' manifests."
    new_codes: [X_JOB_SLOT_LOST, X_QUERY_CACHE_TTL_INVALID]
  - pr: 112
    slices: [02, 06]
    gate: "14 of 17 passed, 3 skipped; app gate every pin holds — examples/dummy 10/17 (7 red,
           7 pinned), social-media-clone 14/17 (3 red, 3 pinned)"
    falsified:
      - "02-H4 said ELEVEN caller-caused codes; it is TEN. X_QUERY_NOT_PAGEABLE is a developer bug
         by its own fix: (an edit to the read's own SQL — nothing the caller sends changes it), and
         X_IDEMPOTENCY_REPLAYED_FAILURE surfaces as itself only when the first attempt carried no
         code. Both got explicit 500 rows so the new check sees a decision, not an omission."
      - "02-H1's 'follow the shape scheduler.ts:153 uses' would have made it WORSE: that line is a
         colon-joined string over framework-controlled values with no actor in it — exactly the
         ambiguous encoding the fix had to avoid. A JSON tuple was used instead."
      - "02-L3's 'SQL_ACK/SQL_NACK in the same file' — they are in jobs/driver-pg-sql.ts, and they
         fence on id AND state because jobs' settle API carries the job id. Idempotency's does not,
         so only the state half was available."
      - "02-M7's prototype description was wrong: pollution fires on the FIRST occurrence, not the
         second, and Object.prototype is never mutated — out['__proto__'] reads a non-undefined
         inherited value, so the repeated-key branch assigns an ARRAY as the object's prototype."
      - "auth and entity were listed as in scope and are FULLY CLOSED — 02-M1 by #104, 02-M6 and
         02-L4 by #106. Remaining entity mentions are pattern citations inside other findings."
    decided:
      - "The completeness step is a RATCHET, not the audit's literal rule. 'Every tier <= 4 code
         needs a row' flags 237 of 394 — a step an agent disables in week one — and reachability is
         not derivable (same tier, same shape, and blanket re-exports collapse the import graph).
         226 codes pinned with a reason per group; the list may only shrink."
      - "It is a fourth rule on the existing `errors` step, not an eighteenth step: VerifyStepName
         is a closed union a generated app inherits, and an app cannot run a check only this repo
         can. Precedent already written at scripts/verify.ts:126-129."
      - "X_LIFECYCLE_DRAINED got a 500 ROW rather than a backlog pin. 500 asserts no reachability
         the way 503 would, and a row records a decision where a pin records indecision. Precedent:
         X_CORS_CONFIG_INVALID, X_RATE_LIMIT_NOT_SHARED."
    deferred:
      - "action's stableStringify still folds -0/NaN/Infinity and CANNOT take query's fix: it is
         also the OpenAPI document serializer, so emitting the bare token NaN would make a published
         spec invalid JSON. Needs the spec serializer split from the hash canonicalizer."
      - "invoke()'s cache bust is not transaction-aware — a real design change and a db seam tier-3
         action deliberately does not import. No tracked app calls transaction( in apps/."
      - "admin (tier 5) is outside the completeness check although X_ADMIN_DENIED reaches a browser;
         the dashboard is dev-only and refused in prod, so ~80 more pins would buy no signal."
      - "The last idempotency race (a straggler landing while the REPLACEMENT reservation is in
         flight) needs the reservation id in settle — a public IdempotencyStore signature change."
      - "reference-app-gate.ts drops the failing shard's stdout, so an app-gate failure names no
         test. Diagnosing one flake cost a standalone re-run plus reading the app's test sources."
    new_codes: [X_IDEMPOTENCY_KEY_INVALID, X_LIFECYCLE_DRAINED, X_ERROR_STATUS_MISSING,
                X_ERROR_STATUS_BACKLOG_STALE, X_ERROR_STATUS_UNKNOWN_CODE]
  - pr: 113
    slices: ['03']
    gate: "14 of 17 passed, 3 skipped; app gate every pin holds — examples/dummy 10/17 (7 red,
           7 pinned), social-media-clone 14/17 (3 red, 3 pinned)"
    falsified:
      - "Unifying render's JSON-LD escaping with seo's would have made it WEAKER: seo handles `</`
         but not `<!--<script>`, which moves the tokenizer into double-escaped state where the
         element's own `</script>` no longer closes it — and it applies the CODE rule to a JSON body
         where the total \\uXXXX rule is strictly safer."
      - "admin -> mcp is NOT a sideways import. admin is tier 5, mcp is tier 4 — downward, and mcp
         was already a runtime dependency of admin. The shared SQL guard needed no new home."
      - "The admin catalog was missing 69 keys, not 27. Twenty travel as DATA (titleKey, labelKey,
         reason literals reached through t(variable)) and are invisible to any extractor that reads
         only t() arguments."
      - "ROLE=migrate was never broken. runRole reads options.env and nothing reads argv, so every
         documented release-phase promise holds; only appended argv is silently discarded."
      - "The budgets pins CANNOT go green from this slice: cmd-verify reads .x/build-stats.json and
         never produces it, .x/ is gitignored, and the app gate never runs `x build`. No gate event."
      - "`lines` being absent from the --json payload IS the contract, obeyed by 23 of 24 call
         sites. The violator was cmd-build.ts, which put the builder's output only there — so CI got
         X_BUILD_FAILED and nothing to act on."
      - "docker/docker-compose.prod.yml does NOT carry the scaffold's bug. The framework image's
         ENTRYPOINT is the CLI binary itself, so `command:` arrives correctly; only the scaffolded
         app's entrypoint reads ROLE and ignores argv. Verified before editing; no change made."
      - "The routed `invalidations: unwired` finding is FALSE: the stub is real text but provably
         overridden by dev-dashboard.ts, the only path x dev takes. A real line, a wrong claim."
      - "Two slice-03 findings were already fixed by #101 — testing/harness.ts's close() and
         template-db.ts's unconditional migrate."
    decided:
      - "pwa's four findings are NOT fixed. It is 3,407 LOC with only planIcons reachable and stays
         unwired by decision, so fixing internals of code with no callers is dead work."
      - "The two new tier-4 codes were PINNED, not given status rows, and the repo's own prior
         answers decided it: X_MAIL_ADDRESS_INVALID beside X_MAIL_HEADER_INVALID (the CR/LF gate on
         headers vs the envelope — same class), X_MCP_RESOURCE_DUPLICATE beside
         X_MCP_TOOL_DUPLICATE (both registration-time, never per-request)."
      - "seo's renderHeadTags DELETED rather than made to delegate: seo is tier 1 and render tier 4,
         and the two doctrines are opposites — xml.ts escapes INTO entities, which is exactly wrong
         inside a raw-text element. A tier-1 export named 'escape script content' would invite the
         very trap being removed."
      - "admin's local SQL guard deleted rather than extended: it covered three of five forms, so it
         produced two explanations for one failure mode, and its message called a $tag$ body and a
         block comment 'a quote'. It was shadowing a BETTER message with a vaguer one."
    deferred:
      - "The inline mail path never runs mailMessageSchema. One call closes it, but it would newly
         reject every send whose locale/tz/url fails validation — blast radius is both tracked apps
         and every scaffold."
      - "LCP is never measured for ANY render mode, so an lcp-only budget passes silently once a
         route has a stats row. Needs a browser, i.e. the e2e step's domain."
      - "runRole is not argv-aware. The complete fix spans the CLI and both tracked apps' generated
         server.ts, so it cannot land in one file set."
      - "seo's renderLd is a third uncalled export, but it returns data and escapes nothing, so it
         carries no vulnerability. Dead-code slice."
      - "TEST_EXCLUSION lacks .tsx — latent; zero .test.tsx files exist, and changing it fails
         package-shape for all 28 published packages until every files array is edited."
    new_codes: [X_CATALOG_KEY_UNREACHABLE, X_MAIL_ADDRESS_INVALID, X_MCP_RESOURCE_DUPLICATE]
  - pr: 114
    slices: ['11', '14']
    gate: "14 of 17 passed, 3 skipped; app gate every pin holds. CodeRabbit reviewed for real —
           25 comments, first unthrottled in four PRs — and a second commit answers them"
    falsified:
      - "The two worst defects were in NEITHER slice doc, and both needed EXECUTION not reading:
         the shipped image was dead on arrival (musl binary on a glibc runtime, every container
         exiting `exec: no such file or directory`) while the build stayed green because the
         --version guard ran on the BUILD image; and RENDER.app = 'stream' meant every generated
         app/ route threw X_ROUTE_MODE_INVALID at import and registered NO ROUTE AT ALL."
      - "`lint` was red from ONE TRAILING BLANK LINE in an emitted test file. Both named causes
         (Bun.env['SITE_ORIGIN'], the biome $schema pin) are Biome INFOS — `biome check` exits 0
         with all three present."
      - "docker/Dockerfile has NO HEALTHCHECK at all and never had; the four byte-identical /readyz
         ones are in the scaffold template and the two apps."
      - "The grace periods ALREADY satisfied the 25s drain deadline — 45s in the chart, 30s in
         compose. No value changed."
      - "ROLE=migrate was never broken: runRole reads options.env and nothing reads argv, so every
         documented release-phase promise held. Only appended argv was discarded."
      - "The budgets pins CANNOT go green from this slice, and measuring inside the step would make
         it worse: social-media-clone prerenders in 8.2s but examples/dummy FAILS in 5.9s
         (X_PRERENDER_FAILED on /offline), so the step would report a build failure under the name
         'budgets'. Skipping was rejected too — the code documents that as the false green an
         earlier wave closed."
      - "`x docs` works, X_DB_DRIFT is not circular, the `Declared` alias does not exist, `✗` on
         success was fixed long ago, and the introspection-command noise does not reproduce in
         either tracked app. Five findings dropped."
      - "helm pullPolicy: IfNotPresent is not a defect. The real bug behind it was appVersion 0.0.1
         naming a tag that has never existed."
    decided:
      - "Eight claims the repo made about itself are now build errors. Two are shaped by what a
         NAIVE version would have broken: the table checker splits on UNESCAPED pipes only (36 rows
         across 16 wiki pages carry an escaped one), and the image rule checks libc FAMILY, not
         base-image version, because a tag->glibc table fails on a correct Dockerfile the day the
         base rebases."
      - "X_IMAGE_GUARD_MISSING is deliberately NOT 'the file contains a guard' — the Dockerfile that
         shipped the dead image HAD one, in the wrong stage, which is how it survived review. The
         rule is that the final stage's ENTRYPOINT must be executed by a RUN in THAT stage."
      - "TWO proposed gate rules were KILLED BY MEASUREMENT. An invented-verb rule flags 21 fix
         lines, 20 of them false positives — including `open the route to <host>:<port>, then:
         x doctor db`, the same word in the same position, correct. A helm render assertion would
         reproduce the fail-open shape at a seam with no skip accounting, and its assertions are
         snapshot counts."
      - "A shared helper for the policy-missing message was argued down: it saves 24 characters,
         adds an import to three packages, and prevents nothing, because the next author still has
         a name in scope and a hole in the sentence."
    deferred:
      - "ENFORCEMENT GAP: all 29 packages/*/tsconfig.json exclude src/**/*.test.ts and typecheck is
         tsc -b, so THE GATE'S FIRST STEP READS ZERO TEST FILES. Same gap as #101's carried '282
         typecheck errors across 110 test files', now corroborated from the other end."
      - "ENFORCEMENT GAP: citedCommandProblem judges the next BARE WORD as a subcommand and never
         inspects FLAGS, so X_PRERENDER_FAILED's `x build --route <path>` passes while --route does
         not exist."
      - "ENFORCEMENT GAP: a fix: containing can('…')/allow('…') whose argument is neither a literal
         resource:verb nor a placeholder. The same mistake occurred in action, query AND admin."
      - "A helm .live. suite with its step in x.verify.json is the honest home for chart coverage —
         branch-shaped assertions only, never the 19-object count."
      - "examples/dummy's /offline page calls useMutationQueue() at prerender, so x build --target
         static cannot complete on the reference app."
      - "x deploy --method compose ignores --image entirely; closing it spans docker/ and templates."
    human_steps_owed:
      - "npm publish -w @ultimat3/flags --access public, BEFORE the next release. The workflow
         publishes tier by tier under set -e and flags is tier 1, so core and schema land on the
         registry at the new version before the run dies: an IRREVERSIBLE PARTIAL RELEASE, 2 of 29
         packages ahead, unpublishable."
      - "Create the npm-publish environment with required reviewers. A workflow referencing an
         environment that does not exist AUTO-CREATES an empty one with no protection rules and
         publishes ungated, while the YAML still reads as gated."
      - "Add the v* deployment tag rule."
      - "Set Trusted Publisher Environment = npm-publish on npmjs.com, per package. Blank today,
         which means a token from ANY run of release.yml is accepted."
    new_codes: [X_PUBLISH_LIST_INCOMPLETE, X_PUBLISH_LIST_UNKNOWN, X_BENCH_CLAIM_STALE,
                X_WIKI_TABLE_MALFORMED, X_FRAME_DOCS_STALE, X_CHART_VERSION_STALE,
                X_IMAGE_LIBC_MISMATCH, X_IMAGE_GUARD_MISSING]
  - pr: 116
    slices: ['08']
    gate: "14 of 17 passed, 3 skipped; app gate every pin holds. `bun install` re-linked two new
           workspace deps — a package.json dep without a re-link is an unresolvable module"
    falsified:
      - "'Two ambient timezone stores' is ONE AND A HALF, and that is why it survived four releases:
         i18n's key was already `locale`, the field core declares and http writes, so that half
         worked end to end. Only time's `timeZone` diverged from core's `tz`. Proven, not argued —
         in the first ui test run the locale and direction assertions PASSED and only timeZone
         failed. The defect was a field-name divergence in ONE CONSTANT."
      - "'Two permission answers' on the admin routes: MEASURED, ALL FIVE PAIRS AGREED — both sides
         reach permissionsForOperation. The real cost is that the table declares 17 routes and 6 are
         served, and the four list pages were byte-identical, so the next page gets written by
         COPYING one — and copying permission:'admin:read' onto an edit screen is a write behind a
         read gate, with the table declaring admin:write for it."
      - "'Delete AdminRouteConfig' would have broken the deployed demo: ops/page.tsx imports it, and
         ops/page.tsx is the ONE PAGE ALREADY MIGRATED to the pattern the fix wanted adopted. The
         prescribed migration was also impossible — assertPath refuses a page path a generated route
         already claimed, and all four are generated."
      - "C4's `enforce: false` was already fixed by the security slice, and DIFFERENTLY: it became
         `unenforced: string`, a mandatory written reason. Following the doc literally would have
         meant deleting something already gone."
      - "The last cycle recorded action's -0/NaN fix as BLOCKED by the OpenAPI duty. Not blocked —
         the duties differ in exactly one branch."
      - "packages/render/src/budgets.ts DOES NOT EXIST; the dead budget surface is seo's. render's
         callerless checkBudgets is in islands.ts and is still homeless."
      - "tagKeys 'in three places' was two copies plus a DIFFERENT FUNCTION under the same name
         (render/src/route.ts, order-preserving, pinned by dsl.test.ts). contentHash 'in three
         places' is three different signatures."
    decided:
      - "Ten of ~13 named deletions were public API. Every removal cleared four points first:
         exported? who imports it (production vs test-only vs its own test)? named in prose? declares
         an X_* code, which is stable forever?"
      - "KEPT against the slice: configureTime/timeConfig/resolveTimeZone (dead ONLY because http
         duplicated them; deleting resolveTimeZone re-opens a documented 31MB formatter-cache leak),
         createCacheStack (it BECOMES the production read path), app-tool.ts (the reference app uses
         the record form, and the ninth-primitive premise fails)."
      - "noiseAt deliberately NOT exported, against the slice's explicit instruction: after the fold
         there is one consumer and db/CLAUDE.md forbids it importing this package, so the export
         would have zero callers on day one — 'exactly how readonly.ts got here'."
      - "X_SEO_BUDGET_EXCEEDED and X_READONLY_VIOLATION RETIRED, not deprecated: a deprecated code
         still answers `x errors explain` with a fix for a check that does not exist. Rows moved
         under Reserved codes so an old log line still resolves."
      - "packages/http/CLAUDE.md's 'May import: core, schema. Nothing else, ever.' is RETIRED — it
         stated no reason, was stricter than the executable tier table, and package-author.md names
         that exact anti-pattern. What it bought was three divergent re-implementations."
    deferred:
      - "C5+C8 — the prod server wrapping the whole dev CLI, and realtime's single barrel serving
         browser and wire-protocol code. Boundary surgery, own PR."
      - "scripts/boundaries.ts CHECKS ONLY THE TIER CEILING, NEVER THE FLOOR — which is why http sat
         at tier 2 importing tier 0 only and nothing noticed for four releases. A floor check would
         have made this slice unnecessary."
      - "@ultimat3/seo's GATE HALF IS ENTIRELY UNWIRED, not just budgets: assertMeta, validateMeta,
         buildSitemap, buildRobots, isIndexable, indexableRoutes, expandRoute, RouteRecord have ZERO
         importers anywhere. NOT deleted — unlike budgets they have no live equivalent, so deleting
         deletes a capability. The docs no longer claim x verify fails CI on them."
      - "AppToolArgs.input is typed InferOutput<TInput> and NO PARSE EVER RUNS, so t.string().email()
         is enforced for an action and silently not for a hand-written tool. One-line fix needs
         validateInput exported from @ultimat3/action."
      - "canonicalJson/fingerprint/isJsonObject are now written THREE times identically across
         action, query and realtime — all tier 3, so the only shared home is core."
      - "core's AppConfig.locales is the last remaining second declaration of the supported locale
         set; ISR caches by pathname with no locale in the key (pre-existing); registerRoute is
         still never called with the admin route table."
    retired_codes: [X_SEO_BUDGET_EXCEEDED, X_READONLY_VIOLATION]
  - pr: 117
    slices: ['15']
    gate: "14 of 17 passed, 3 skipped; app gate every pin holds"
    falsified:
      - "The slice frames these as '42 divergences'. In FOUR OF SIX SEAMS the in-memory driver was
         not merely different — it was CONCEALING a defect that only appears in production. Every
         one reads as 'dev works, production does not', which is the exact failure a parity test
         exists to make impossible."
      - "A5's named scenario was ALREADY CLOSED — expired AND claimed by another worker fenced
         correctly on both sides. The live case was expired AND UNCLAIMED, which the audit did not
         name: pg revived it, memory refuses it, and the caller turns a refused renewal into
         X_JOB_SLOT_LOST — so a stalled worker was killed in dev and ran on in production."
      - "A8/A9 are NOT construction-time validation differences; both drivers already validate
         symmetrically there. One is a send-time gate, the other a send-time identifier."
      - "isRecord is SEVEN shipped declarations, not twelve, and purge-http.ts's is NOT public API —
         index.ts re-exports two other symbols from that file and the package has one entry point.
         No BREAKING entry was needed."
      - "query has NO canonicalJson at all, so the 'three identical copies' claim was two. And there
         are FOUR fnv1a declarations, not three — one is named contentHash — so only two of the
         three 'contentHashes' are content hashes; render's is FNV-1a, not sha256."
      - "recentTierFailures()'s stated reason to keep was FALSE: the /_x panel does not read it
         (dev-dashboard.ts imports recentInvalidations and nothing else). Kept for a different
         reason — it is the only evidence a degraded tier leaves."
      - "list()'s 'must not read every file' comment was false AND worse than reported: copy()
         inherited the same read, and get() was reading its file twice."
    decided:
      - "Mail refuses at the SEND, not at boot, and only outside development/test. Boot refusal
         turns a working deploy of a mail-less app into a failing one; refusal at send lands on the
         path that needed the capability, where the queue's retry and dead-letter paths already
         know what to do with it."
      - "DECLINED 'make s3 signedUrl refuse maxBytes' — grant.ts passes it on EVERY grant, so it
         would throw on every s3 upload in every app. DECLINED 'make local refuse metadata' — it
         would delete a working tested capability to buy symmetry with a temporary Bun limitation.
         Both halves pinned instead, so the s3 line fails when Bun grows the hook."
      - "MemoryAdapter not enforcing email uniqueness STAYS, as a Known-Gaps row: every fix mints a
         refusal Postgres does not throw, and the defensible home is a check in register() — an
         ACCOUNT-ENUMERATION decision, which auth's whole loginFailed() doctrine exists to avoid."
      - "Six parity suites, and the shape differs by seam: four run both drivers FOR REAL; jobs and
         auth assert the SQL that would go out — and auth's asserts BOUND VALUES, not text, which is
         stronger than the shape it was copied from."
    deferred:
      - "The identity-function consolidation needs @ultimat3/core (tier 0) and must resolve query's
         missing document/hash split FIRST, or it publishes bare NaN tokens into a document surface.
         Surveyed with measured facts so it lands as a decision rather than a grep."
      - "The upload ceiling on a bucket-backed disk: a presigned S3 PUT cannot carry maxBytes and
         grantUpload promises it regardless. The two places that could enforce it are a mounted
         accept path (which does not exist) or a post-upload object.size check in promoteAttachment."
      - "Wiring the /_x panel to recentTierFailures, in cli/src/dev-dashboard.ts."
    new_codes: [X_MAIL_CREDENTIAL_MISSING, X_STORAGE_LIST_FAILED]
  - pr: 118
    slices: ['12']
    gate: "14 of 17 passed, 3 skipped; app gate every pin holds — examples/dummy 10/17 -> 11/17
           (7 pins -> 6), social-media-clone 14/17 -> 15/17 (3 pins -> 2)"
    falsified:
      - "examples/dummy pinned contract, live and job with ONE stated cause — an unscoped repo —
         and it was WRONG FOR ALL THREE. The repo scopes correctly. Real causes: the seed fixture
         wrote into a PRIVATE DRIVER nothing else in the process could read, so every action's row
         loader saw an empty table; and the actor factory minted a USER id where this app's identity
         is the MEMBERSHIP, so an actor owned nothing it wrote and the policy denied an author their
         own draft."
      - "posts/actions.ts claimed the rule compares the org 'before it reads the row at all'. It
         does not — invoke.ts awaits the row loader THEN calls the guard. That inverted comment about
         ORDERING is why three investigations went to the wrong file."
      - "Flipping the demo's driver was necessary and nowhere near sufficient: the committed
         migration created ZERO TABLES (five lines, all comments), and the REGENERATED one could not
         apply either — x db gen emits create table in entity-registration order with foreign keys
         inline, so `blocks references users` came first. FRAMEWORK BUG, unfixed and reported."
      - "/admin/jobs was NOT missing a route — defineAdmin builds it for every app; only the NAV
         ITEM was missing, and adminNav() never derives from built-in routes."
      - "repo.ts CANNOT call decideOperation — admin.ts imports it, so the gate went at the caller.
         And there is no leak today: every role carries media:read beside job:read."
      - "Fixing the packages/mcp upward import does NOT turn boundaries green — measured before and
         after, the same 3 findings. readAppSources globs only apps/*/{site,app,api,shared}/**, so
         AN APP'S OWN packages/ ARE OUTSIDE THE BOUNDARY CHECK ENTIRELY. Framework gap."
      - "The offline-page fix is a HARD PRECONDITION, not a closer: x build --target static now
         completes and weighs 3 of 8, but the other 5 fail for reasons no app can reach (APP_URL
         unset; the app/ shell rendering outside a request). budgets stays pinned with corrected text."
      - "config.ai.mcp is read by NOTHING and packages/mcp has zero importers — the demo serves NO
         MCP ENDPOINT AT ALL."
    decided:
      - "Two pins DELETED (examples/dummy:contract, now 14/14; social-media-clone:drift, now green)
         and three REWRITTEN from false causes to real ones. A pin whose stated cause is wrong is
         worse than no pin: it is a documented claim nothing verifies, inside the gate itself."
      - "Production without DATABASE_URL now REFUSES rather than serving four disconnected in-memory
         worlds. Precedent: startStorage's LocalDiskUnsafeError."
      - "DECLINED an island for the offline queue count: the string is a CLDR plural and island props
         cross the seam ALREADY TRANSLATED, so a count only the browser knows cannot be
         pre-translated. Used the hasLiveClient() guard the update banner already uses."
      - "DECLINED sync:true on a production send so a test could observe a step failure — it is
         documented 'tests and CLI one-shots only', and this is the REFERENCE app. Retargeted the
         assertion at the mail job's retry, where the retry actually happens."
      - "DECLINED branching examples/dummy's driver on DATABASE_URL: its harness reaches the app's
         store THROUGH defaultDriver() precisely because the client names none."
    deferred:
      - "x db gen emits no topological sort, so a first migration with foreign keys cannot apply.
         Same generator drops non-timestamp column defaults, so the database does not enforce what
         the entity declares."
      - "An app's own packages/ are never boundary-checked (cli/src/app-boundaries.ts)."
      - "The memory driver ignores includeDeleted on writes while pg-driver honours it — a soft-
         deleted row is addressable on Postgres and not in memory."
      - "registerErrorStatus is called nowhere in the demo app, so every app code answers 500 —
         including X_CREDENTIALS_INVALID, which should be 401."
      - "No app seam installs channel guards; the hub is built by the socket process and never asks
         the app. Both apps document the same gap independently."
    new_codes: [X_ACTOR_UNRESOLVED, X_ADMIN_ACTION_UNKNOWN, X_ADMIN_ACTION_REFUSED]
  - pr: 119
    slices: ['13']
    gate: "14 of 17 passed, 3 skipped; app gate every pin holds — examples/dummy 11/17,
           social-media-clone 15/17. Manifest at 417 codes (405 + 12 new gate codes)"
    falsified:
      - "packages/action/README.md was RIGHT and the code is wrong. One action is reachable as
         `publishPost` on the wire (mcp/from-action.ts:74 — the only name tools/call accepts) while
         openapi.json AND x.manifest.json both publish `publish_post`. THE PUBLISHED CONTRACT
         ADVERTISES A NAME NOTHING SERVES. Not unified — BREAKING, touches both apps' committed
         openapi.json and nine test files. Filed as #120."
      - "packages/ai/src/eval-errors.ts does NOT cite `x test summarize` — it reads
         `x test eval --filter`. The convention was being held BY MEMORY; positionalChoices makes it
         a build error. Only the README still printed the bad form."
      - "createPgLeader EXISTS (driver-pg.ts:383), contrary to the report that it does not. The
         defect was that scheduler.ts:42 recommended it — the session-scoped one whose grant dies
         with the pooled connection — over createPgLeaseLeader. ELEVEN files described leader
         election as an advisory lock, one with a pg_try_advisory_lock block."
      - "Most of the slice's High table was ALREADY FIXED in-flight by the eighteen PRs merged since
         it was written. Each verified and dropped. The new checks would have caught every one."
      - "The scaffold count is 115/91, MEASURED — not the wiki's 99/76 and not the slice document's
         own 112/88. The audit's replacement number was as stale as the number it replaced."
      - "docker/README.md's two rows were already correct — the healthz claim was fixed and the
         dependency stage really is lockfile-only (a `manifests` stage strips the rest). Dropped."
      - "`x env check` was documented as PLANNED and it ships — a working diagnostic every
         X_ENV_MISSING fix line points at, documented as unavailable."
      - "docs/architecture/02-boundaries.md documented two boundary escape hatches with ZERO
         implementation anywhere, and named schema->core as the sideways exception when
         SIDEWAYS_ALLOW has no such entry and schema/src/errors.ts says the opposite."
    decided:
      - "23 wiki version stamps collapsed to ONE. Not find-and-replaced to 1.2.0: [Unreleased]
         carries 30 BREAKING entries so the next cut is a major and all 23 would be wrong again in
         one release. _Footer.md renders on every page, so it is the one page that states it."
      - "The flag half of the citation resolver SHIPS ENFORCING — 386 flag citations, 23 flagged,
         0 false positives. The bar was a measurement, not optimism; a sibling agent correctly
         declined a similar rule at 21 flagged / 20 false positives earlier in this audit."
      - "readme-fences ships PINNED, not enforcing: 170 fences across 28 of 29 READMEs, 155 failing,
         most illustrative fragments. A ratchet that only lowers means a NEW broken example fails
         from day one while the existing 155 retire. The slice framed this as three READMEs."
      - "Four legitimate negation sentences ('there is no `x env --fix`') went into
         DOC_COMMAND_ALLOWANCES rather than being deleted. Refusing them would delete true
         sentences, which is a worse doc than a stale one."
    deferred:
      - "#120 — the MCP tool name split. #121 — a fresh app cannot run four of its generators and
         its first migration is wrong four ways. #122 — two shipped fix: lines that resolve and
         still do the wrong thing; one CREATES a database."
      - "scaffold-smoke never runs a generator and never runs `x db migrate`, so every defect in
         #121 is outside what CI exercises."
      - "Six conventions documented as rules that nothing enforces are now LABELLED rather than
         asserted: hardcoded-string, raw-hex, date-no-tz, money-as-number, SW checksum, strategy
         coherence. Each names the check that would close it."
      - "CHANGELOG.md has no [1.1.0] or [1.2.0] section — only [Unreleased], [1.0.0], [0.0.1] —
         while npm serves 1.1.0 and 1.2.0."
    new_codes: [X_DOC_FIX_UNRUNNABLE, X_DOC_FIX_UNSCANNED, X_DOC_COMMAND_UNKNOWN,
                X_DOC_COMMAND_ALLOWANCE_STALE, X_DOC_COMMAND_UNSCANNED, X_README_EXAMPLE_UNCOMPILED,
                X_README_EXAMPLE_PIN_STALE, X_README_EXAMPLE_UNSCANNED, X_VERSION_STAMP_STALE,
                X_VERSION_STAMP_DUPLICATE, X_VERSION_LOCKSTEP_BROKEN, X_VERSION_STAMP_UNSCANNED]
notes: >
  Slice order in this file is execution order, not tier order — 05 and 10 land first because the gate
  does not yet mean what it says, and 08 lands late because it is mostly deletions that would collide
  with patches from 01-03. Findings reached by more than one sweep are cross-linked in overview.md
  under "PR shape": fix once, cite both. Two design decisions must be made explicitly before their
  slices can land — job-surface tenancy (07) and MoneyValue.scale (09).
last_updated: 2026-08-16
