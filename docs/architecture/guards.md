# Guards

GENERATED by `bun run scripts/guards-doc.ts --write` from each script’s own header — never
hand-edited; `--check` refuses drift (`X_GUARDS_DOC_DRIFT`). A guard is a runnable
`scripts/*.ts` that emits at least one code. Which gate step runs each one is
`scripts/verify.ts` and `VERIFY_STEP_NAMES`; the codes are in
[`wiki/Error-Codes.md`](../../wiki/Error-Codes.md).

| Command | What its header says it refuses | Codes |
|---|---|---|
| `bun run async-context-guard` | Enforce, as a build error, that `packages/core/src/async-context.ts` holds the framework's ONE `AsyncLocalStorage`. | `X_ASYNC_CONTEXT_UNAVAILABLE` |
| `bun run scripts/bench-claims.ts` | Enforce, on `x verify`'s `manifest` step, that the realtime capacity figures `scripts/bench/results/README.md` states are the figures the committed bench results carry. | `X_BENCH_CLAIM_STALE` |
| `bun run boundaries` | Enforce the repo's import rules as BUILD ERRORS, not lint warnings (axiom 3). | `X_ADMIN_FLATTENER_VIOLATION` `X_BOUNDARY_SHARED_LEAF` `X_BOUNDARY_VIOLATION` |
| `bun run browser-transport` | Enforce, as a build error, that browser code has ONE HTTP function and ONE socket. | `X_BROWSER_SERVER_BARREL` `X_BROWSER_TRANSPORT_BYPASS` `X_BROWSER_TRANSPORT_UNSCANNED` |
| `bun run budget-raises` | Enforce, as a build error, the half of axiom 9 that is a diff: a route's `budget:` may be RAISED, and the raise carries its measured number and its reason in the same change. | `X_BUDGET_BASE_MISSING` `X_BUDGET_RAISE_UNSTATED` |
| `bun run scripts/catch-render.ts` | Enforce, as a ratchet, that a CAUGHT value does not reach a `cause:` / `fix:` / `detail:` through `instanceof`, `String()`, `JSON.stringify()` or a bare `${…}`. | `X_CATCH_RENDER_PIN_STALE` `X_CATCH_RENDER_UNSAFE` `X_CATCH_RENDER_UNSCANNED` |
| `bun run changelog-check` | Enforce, as a gate rule, that CHANGELOG.md's sections are well-formed and that every migration count in wiki/Upgrading.md is read out of that major's OWN section. | `X_DOC_CHANGELOG_SECTION_INVALID` `X_DOC_CHANGELOG_UNRELEASED_BREAKING` `X_DOC_MIGRATION_COUNT_STALE` `X_DOC_MIGRATION_SECTION_MISSING` `X_DOC_MIGRATION_UNSCANNED` |
| `bun run channel-literals` | Enforce, as a build error, that a channel topic is spelled in ONE place: the `channel(name, …)` declaration in `packages/realtime/src/channel.ts`, whose params builder is the only way to turn a declaration into a topic. | `X_CHANNEL_LITERAL` `X_CHANNEL_LITERAL_UNSCANNED` |
| `bun run scripts/chart-version.ts` | Enforce, as a gate step, that `docker/helm/Chart.yaml` still carries the lockstep version — and give `scripts/release.ts` the one function that keeps it there, so it stops being hand-kept. | `X_CHART_VERSION_STALE` |
| `bun run claude-md-size` | Enforce a byte ceiling on `CLAUDE.md`: 16 KB for the root file, 24 KB for a package's, and a per-file pin (a ratchet that refuses growth only) for the package files already above it. | `X_CLAUDE_MD_OVERSIZE` `X_CLAUDE_MD_UNSCANNED` |
| `bun run scripts/config-readers.ts` | Enforce, as a ratchet, that every leaf key of `AppConfig` has at least one READER in `packages/*/src`. | `X_CONFIG_KEY_READER_AMBIGUOUS` `X_CONFIG_KEY_UNREAD` `X_CONFIG_READERS_UNSCANNED` `X_CONFIG_READER_PIN_STALE` `X_CONFIG_READER_PIN_UNEXPLAINED` |
| `bun run coverage` | Enforce, per package, that a package's own `src/` is covered by its own tests. | `X_COVERAGE_BELOW` `X_COVERAGE_PIN_STALE` `X_COVERAGE_UNMEASURED` `X_TEST_FAILED` |
| `bun run dead-docs-host` | Enforce, as a ratchet, that no shipped source builds a URL on `ultimate.dev`. | `X_DEAD_DOCS_HOST` `X_DEAD_DOCS_HOST_PIN_STALE` `X_DEAD_DOCS_HOST_UNSCANNED` |
| `bun run declaration-readers` | Enforce that every leaf key of every DECLARATION an app writes — the type each primitive and each `define*` factory takes — is read by something in `packages/*/src`. | `X_DECLARATION_READERS_UNSCANNED` `X_DECLARATION_READER_PIN_STALE` `X_DECLARED_KEY_UNREAD` |
| `bun run doc-commands` | Enforce, as a gate step, that a command a doc page hands a reader is a command this build ships. | `X_DOC_COMMAND_ALLOWANCE_STALE` `X_DOC_COMMAND_PIN_EXCEEDED` `X_DOC_COMMAND_PIN_STALE` `X_DOC_COMMAND_UNKNOWN` `X_DOC_COMMAND_UNSCANNED` |
| `bun run scripts/doc-config-keys.ts` | Enforce, as a gate step, that documentation citing an `app.config.ts` key names a key that EXISTS. | `X_DOC_CONFIG_ALLOWANCE_STALE` `X_DOC_CONFIG_KEY_UNKNOWN` |
| `bun run scripts/doc-fixes.ts` | Enforce, as a gate step, that the `Fix` column of `wiki/Error-Codes.md` is runnable — the same rule `x verify`'s `errors` step already applies to every `fix:` in shipped source. | `X_DOC_FIX_UNRUNNABLE` `X_DOC_FIX_UNSCANNED` |
| `bun run scripts/error-map.ts` | Enforce, as a gate step, that `packages/http/src/error-map.ts`'s status table stays CLOSED. | `X_ERROR_STATUS_BACKLOG_STALE` `X_ERROR_STATUS_MISSING` `X_ERROR_STATUS_UNKNOWN_CODE` |
| `bun run error-render` | Enforce, as a build error, that an error factory cannot die formatting its own message. | `X_ERROR_RENDER_UNSAFE` |
| `bun run finite-bounds` | Enforce, as a ratchet, that a NUMERIC option defaulted with `??` is checked for finiteness. | `X_FINITE_BOUND_PIN_STALE` `X_FINITE_BOUND_UNCHECKED` `X_FINITE_BOUND_UNSCANNED` |
| `bun run fix-shell-arg` | Enforce, as a ratchet, that a value spliced into a `fix:` line never lands in a SHELL COMMAND POSITION unscreened. | `X_FIX_SHELL_ARG_PIN_STALE` `X_FIX_SHELL_ARG_PIN_UNEXPLAINED` `X_FIX_SHELL_ARG_UNSCANNED` `X_FIX_SHELL_ARG_UNSCREENED` |
| `bun run flight-copies` | One rule, two halves: the framework computes a retry delay in ONE place, and it never rolls a die a test cannot control. | `X_FLIGHT_RANDOM_UNINJECTED` `X_FLIGHT_SECOND_CURVE` |
| `bun run framework-tables` | Enforce, as a ratchet, that every framework table a package DECLARES is a table some boot path CREATES — `FRAMEWORK_SCHEMA` in `@ultimat3/cli` being the one applier the framework has. | `X_FRAMEWORK_TABLE_UNAPPLIED` `X_FRAMEWORK_TABLE_UNSCANNED` |
| `bun run frozen-records` | One rule: `Object.freeze` over an object literal whose type is a CLOSED-KEY `Record` must pass that type as an explicit type argument — `Object.freeze<Record<K, V>>({…})`, never `const X: Readonly<Record<K, V>> = Object.freeze({…})`, which infers `T` from the literal. | `X_FROZEN_RECORD_INFERRED` `X_FROZEN_RECORD_UNSCANNED` |
| `bun run gate-codes` | Enforce, as a gate rule, that `wiki/Error-Codes.md`'s "never ships to an app" parenthesis is complete in both directions: every code it names has a table row, and every code `scripts/` declares is named in it. | `X_GATE_CODE_BACKLOG_STALE` `X_GATE_CODE_UNDOCUMENTED` |
| `bun run gate-steps` | Enforce, as a gate step, that a page stating how many steps `x verify` runs — or enumerating them — states the list this build actually ships. | `X_DOC_GATE_STEPS_STALE` `X_DOC_GATE_STEPS_UNSCANNED` |
| `bun run scripts/generator-counts.ts` | Enforce, as a gate step, that every file count a doc states about a generator is a number that generator still produces. | `X_DOC_FILE_COUNT_STALE` `X_DOC_FILE_COUNT_UNSCANNED` |
| `bun run guards-doc` | Generate `docs/architecture/guards.md` from the guards' own headers — command, what it refuses, the codes it emits — and refuse a committed page that has drifted from them. | `X_GUARDS_DOC_DRIFT` `X_GUARDS_DOC_UNSCANNED` |
| `bun run scripts/i18n-catalog.ts` | Enforce, as a gate step, that `packages/i18n/src/catalogs/en.json` still describes the framework packages' own strings. | `X_CATALOG_KEY_UNREACHABLE` `X_CATALOG_MISSING_KEYS` |
| `bun run scripts/image-contract.ts` | Enforce, as a gate step, the two properties that make `docker/Dockerfile` produce an image that can start. | `X_IMAGE_GUARD_MISSING` `X_IMAGE_LIBC_MISMATCH` `X_IMAGE_SECRET_UNIGNORED` |
| `bun run index-of-order` | Refuse an ordering assertion built on `indexOf`/`findIndex` with nothing asserting the needle is PRESENT — the shape of a test that cannot fail. | `X_INDEX_ORDER_PIN_STALE` `X_INDEX_ORDER_PIN_UNEXPLAINED` `X_INDEX_ORDER_UNGUARDED` `X_INDEX_ORDER_UNSCANNED` |
| `bun run llms-txt` | `llms.txt`'s two derived lists, generated: the packages (from `listWorkspaces` and each package's own `description`) and the wiki pages (in `wiki/_Sidebar.md`'s order, summarised by `wiki/Home.md`'s tables). | `X_LLMS_TXT_DRIFT` |
| `bun run lockfile` | Bring `bun.lock`'s recorded `@ultimat3/*` ranges back into agreement with the `package.json` files it was generated from — the edit `X_LOCKFILE_STALE` names, performed. | `X_LOCKFILE_STALE` |
| `bun run new-error-code` | Add an error code in ONE edit: its registration in the owning package's `errors.ts` and its row in `wiki/Error-Codes.md`, written together or not at all. | `X_NEW_ERROR_CODE_INVALID` |
| `bun run scripts/new-package.ts` | Scaffold a framework package from the contract's templates: the same eight files, the same package.json shape, a real error class and two tests that would catch a regression. | `X_CLI_BAD_FLAG` `X_GENERATE_CONFLICT` |
| `bun run node-imports` | Enforce, as a ratchet, that every `node:` import carries a `why:` comment on it or directly above. | `X_NODE_IMPORT_PIN_STALE` `X_NODE_IMPORT_UNEXPLAINED` `X_NODE_IMPORT_UNSCANNED` |
| `bun run package-map-graph` | Enforce, as a gate rule, that the dependency graph `docs/architecture/01-package-map.md` DRAWS is the dependency graph the manifests declare: every arrow is a dependency the `from` package's own `package.json` holds, and every publishable workspace is a node on it. | `X_DOC_PACKAGE_GRAPH_STALE` `X_DOC_PACKAGE_GRAPH_UNSCANNED` |
| `bun run pin-raises` | A ratchet pin that goes UP needs its reason in the same diff — `budget-raises`' rule, applied to every `scripts/lib/*-pins.ts` table. | `X_PIN_BASE_MISSING` `X_PIN_RAISE_UNSTATED` |
| `bun run proto-index` | Enforce, as a ratchet, that a plain object literal typed `Record<…>` is never READ with a computed key that is not a string literal. | `X_PROTO_CHAIN_INDEX` `X_PROTO_CHAIN_INDEX_PIN_STALE` `X_PROTO_CHAIN_INDEX_PIN_UNEXPLAINED` `X_PROTO_CHAIN_INDEX_UNSCANNED` |
| `bun run scripts/readme-fences.ts` | Enforce, as a gate step, that a fenced `ts`/`tsx` example in a package README typechecks. | `X_README_EXAMPLE_PIN_STALE` `X_README_EXAMPLE_UNCOMPILED` `X_README_EXAMPLE_UNSCANNED` |
| `bun run scripts/reference-app-gate.ts` | Every tracked app's own `x verify`, built first as its `bin/check` does, run as a BLOCKING check on this repo — `examples/dummy` and the deployed `dummy/social-media-clone`. | `X_CLI_BAD_FLAG` `X_REFERENCE_APP_NO_FLOOR` `X_REFERENCE_APP_PIN_STALE` `X_REFERENCE_APP_REGRESSED` `X_REFERENCE_APP_UNREFERENCED` |
| `bun run scripts/registry-audit.ts` | Compare the REGISTRY to this tree: for every publishable workspace, is it on npm at all, is it there at the version this tree stamps, and was that version published by the workflow. | `X_REGISTRY_BOOTSTRAP_OWED` `X_REGISTRY_UNATTESTED` `X_REGISTRY_UNREACHABLE` `X_REGISTRY_VERSION_BEHIND` |
| `bun run scripts/release-facts.ts` | Enforce, as a gate step, that the two release-availability COUNTS this repo restates across ten pages are the counts the tree actually has. | `X_DOC_RELEASE_FACT_STALE` `X_DOC_RELEASE_FACT_UNSCANNED` |
| `bun run scripts/release-workflow.ts` | Enforce, as a gate step, that `.github/workflows/release.yml` still publishes EVERY publishable workspace. | `X_PUBLISH_LIST_INCOMPLETE` `X_PUBLISH_LIST_UNKNOWN` |
| `bun run scripts/release.ts` | Lockstep versioning: every published package moves to the same version in one commit. | `X_CLI_BAD_FLAG` `X_RELEASE_FLAG_UNKNOWN` `X_RELEASE_VERSION_INVALID` `X_RELEASE_VERSION_SKEW` `X_RELEASE_VERSION_UNSTATED` |
| `bun run render-modes` | One rule: a closed set of string literals is declared in ONE module and nowhere else. | `X_VOCABULARY_REDECLARED` `X_VOCABULARY_UNSCANNED` |
| `bun run scripts/roadmap.ts` | Turns `docs/idea/14-roadmap.md` from prose into a gate step. | `X_ROADMAP_FILE_MISSING` `X_ROADMAP_MILESTONE_UNTRACKED` `X_ROADMAP_MILESTONE_UNVERIFIED` `X_ROADMAP_STATUS_MISSING` |
| `bun run scripts/scaffold-first-run.ts` | EVERY GENERATOR, run over an app whose `bin/setup` has already run. | `X_CLI_BAD_FLAG` `X_SCAFFOLD_FIRST_RUN_FAILED` |
| `bun run scripts/scaffold-gate.ts` | A freshly scaffolded app's OWN `bin/setup && bin/check`, run as CI's check — the two commands a dz-runner box runs on an Ubuntu VM with bun and git and nothing else, and the contract the platform depends on. | `X_CLI_BAD_FLAG` `X_SCAFFOLD_FIRST_RUN_FAILED` `X_SCAFFOLD_GATE_RED` `X_SCAFFOLD_VERIFY_UNREADABLE` |
| `bun run scripts/scaffold-smoke-overrides.ts` | The scaffold smoke job (`.github/workflows/ci.yml`, job `scaffold-smoke`) proves `x new` → `bun install` → `x verify` works end to end, without waiting on an actual npm publish: it rewrites the freshly scaffolded app's `@ultimat3/*` ranges to `file:` overrides pointing at this repo's own `packages/*`, so `bun install` links the same source a real publish would ship instead of hitting the registry. | `X_CLI_BAD_FLAG` `X_SCAFFOLD_OVERRIDES_EMPTY` |
| `bun run secret-compare` | Enforce, as a ratchet, that a value whose NAME says it is a secret is never compared with an operator that stops at the first differing byte: `===`, `!==`, `.includes()`, `.indexOf()`, `.startsWith()`, `.endsWith()`, a `switch` over it, or `Bun.deepEquals()`. | `X_SECRET_COMPARED_UNSAFELY` `X_SECRET_COMPARE_PIN_STALE` `X_SECRET_COMPARE_PIN_UNEXPLAINED` `X_SECRET_COMPARE_UNSCANNED` |
| `bun run scripts/setup-commands.ts` | Enforce, as a gate step, that every page enumerating what a scaffolded app's `bin/setup` runs enumerates the script `x new` actually writes. | `X_DOC_SETUP_COMMANDS_UNSCANNED` `X_DOC_SETUP_COMMAND_STALE` |
| `bun run side-effects` | Two rules, and the second exists because the first was resting on something untrue. | `X_CLI_BAD_FLAG` `X_SIDE_EFFECTS_ANCHOR_STALE` `X_SIDE_EFFECTS_ENTRY_STALE` `X_SIDE_EFFECTS_MISSING` `X_SIDE_EFFECTS_PIN_STALE` `X_SIDE_EFFECTS_UNANCHORED` `X_SIDE_EFFECTS_UNDECLARED` `X_SIDE_EFFECTS_UNSCANNED` |
| `bun run skip-if-cleanup` | Refuse a test file that resets a PROCESS-GLOBAL registry from inside a `.skipIf(` block, where the reset never runs in the configuration the block is skipped in. | `X_SKIP_CLEANUP_UNREACHED` `X_SKIP_CLEANUP_UNSCANNED` |
| `bun run sql-literal-copies` | Enforce that ONE module in this tree turns a value into a SQL string literal. | `X_SQL_LITERAL_COPY` `X_SQL_LITERAL_UNSCANNED` |
| `bun run scripts/test-bare-error.ts` | Enforce, as a gate step, that a test does not report ITS OWN verdict by throwing a bare `Error`. | `X_TEST_BARE_ERROR` `X_TEST_BARE_ERROR_PIN_STALE` `X_TEST_BARE_ERROR_UNSCANNED` |
| `bun run scripts/test-fix-citations.ts` | Enforce, as a gate step, that a `fix:` a TEST writes or asserts is a command this build can run. | `X_TEST_FIX_PIN_STALE` `X_TEST_FIX_UNRUNNABLE` `X_TEST_FIX_UNSCANNED` |
| `bun run scripts/test-typecheck-gate.ts` | Enforce, as a gate step, that this repo's TEST sources typecheck. | `X_CLI_BAD_FLAG` `X_TEST_TYPECHECK_PIN_STALE` `X_TEST_TYPECHECK_REGRESSED` `X_TEST_TYPECHECK_UNSCANNED` |
| `bun run scripts/to-throw-returns.ts` | Enforce, as a gate step, that `expect(fn).toThrow(...)` is given a function that THROWS. | `X_TEST_THROW_NOT_THROWN` |
| `bun run scripts/trust-publishers.ts` | Attaches the OIDC trusted publisher to every published package, and verifies it stayed attached. | `X_TRUST_2FA_REQUIRED` `X_TRUST_ENVIRONMENT_EMPTY` `X_TRUST_NPM_TOO_OLD` `X_TRUST_PUBLISHER_FAILED` `X_TRUST_PUBLISHER_MISSING` |
| `bun run verify` | The gate for the framework repo itself: `x verify`, run at the repo root. | `X_MANIFEST_DRIFT` `X_VERIFY_FAILED` |
| `bun run scripts/version-stamps.ts` | Enforce, as a gate step, the two version facts this repo states in prose and checked nowhere. | `X_LOCKFILE_STALE` `X_VERSION_LOCKSTEP_BROKEN` `X_VERSION_STAMP_DUPLICATE` `X_VERSION_STAMP_PIN_STALE` `X_VERSION_STAMP_STALE` `X_VERSION_STAMP_UNSCANNED` |
| `bun run scripts/wiki-frames.ts` | Enforce, as a gate step, that `wiki/Realtime.md` and the wire protocol still name the same frames. | `X_FRAME_DOCS_STALE` |
| `bun run scripts/wiki-tables.ts` | Enforce, as a gate step, that every markdown table in `wiki/` still renders as a table. | `X_WIKI_TABLE_MALFORMED` |
