# mobile

Placeholder. The monorepo shape exists now so adding mobile later is a new directory, not a
restructure.

| Question | Answer |
|---|---|
| Stack | native Swift / Kotlin against the generated typed client |
| API | the same actions as `apps/web/api` — one authz system, one contract |
| Contract | `openapi.json` at the repo root, regenerated by `x manifest` |
| Start | `x new social-media-clone-mobile` inside this directory, or wire it by hand |
