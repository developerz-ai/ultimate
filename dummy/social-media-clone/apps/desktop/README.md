# desktop

Placeholder. The monorepo shape exists now so adding desktop later is a new directory, not a
restructure.

| Question | Answer |
|---|---|
| Stack | Tauri shell around the app/ surface |
| API | the same actions as `apps/web/api` — one authz system, one contract |
| Contract | `openapi.json` at the repo root, regenerated by `x manifest` |
| Start | `x new social-media-clone-desktop` inside this directory, or wire it by hand |
