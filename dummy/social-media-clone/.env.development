# Committed non-secret defaults. Per-box secrets go in .env.development.local, which wins.
# Empty DATABASE_URL means "embedded": x dev runs PGlite in-process, no Docker required.
DATABASE_URL=
NATS_URL=
S3_ENDPOINT=
PORT=3000
ROLE=web
