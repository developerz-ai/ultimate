# Committed template. Copy to `.env` and fill in — `.env` is gitignored.
# Operator credentials for the PUBLISHING.md human steps only. Routine releases
# use OIDC trusted publishing from GitHub Actions and need none of these.

NPM_TOKEN=
NPM_USERNAME=
NPM_PASSWORD=
NPM_TOTP_SECRET=
NPM_TOTP_URI=
