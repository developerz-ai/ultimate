{
  "name": "@postly/db",
  "version": "0.0.1",
  "description": "Postly's entities, migrations, cache tags and seeds — schema only",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./seeds/dev": "./seeds/dev.ts"
  },
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "gen": "x db gen",
    "migrate": "x db migrate"
  },
  "dependencies": {
    "@postly/domain": "0.0.1",
    "@ultimat3/cache": "18.0.0",
    "@ultimat3/core": "18.0.0",
    "@ultimat3/entity": "18.0.0"
  }
}
