#!/usr/bin/env bash
# The only gate. CI runs exactly this — a check that lives only in CI cannot be run locally.
set -euo pipefail
cd "$(dirname "$0")/.."

exec x verify "$@"   # add --json for a machine-readable report
