{
  "name": "@postly/root",
  "version": "0.0.1",
  "private": true,
  "description": "Postly \u2014 the Ultimate reference application",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "setup": "bin/setup",
    "dev": "bin/dev",
    "check": "bin/check",
    "build": "x build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "db:gen": "x db gen",
    "db:migrate": "x db migrate",
    "db:seed": "x db seed dev",
    "manifest": "x manifest --json"
  },
  "devDependencies": {
    "@ultimat3/cache": "5.0.1",
    "@ultimat3/entity": "5.0.1",
    "@ultimat3/testing": "5.0.1",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "@ultimat3/cli": "5.0.1",
    "@ultimat3/core": "5.0.1"
  }
}
